extends layout

block content
  h1= title
  p Welcome to #{title}
  p This is a test by #{name}.
  p Now is #{dateTime}
  p Random Number: #{randomNum}
  p Param #{param}
  a(href='/users') Users
  p
  a(href='/users/list') Users List
